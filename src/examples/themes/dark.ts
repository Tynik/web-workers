import { createTheme } from '@material-ui/core';

export const darkTheme = createTheme({
  palette: {
    background: {
      default: '#222222'
    },
    text: {
      primary: '#ffffff'
    }
  }
});
